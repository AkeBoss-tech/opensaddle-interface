# Project Run Work and connector audit acceptance — 2026-09-19

The existing personal-runtime acceptance Run `run_0714f2cbd1924c9e9a93bf5ec808bc86` was reused. No second provider turn was made. Before this Interface change, the completed Run was visible in its Project task and result views but the top-level Work page showed zero entries; the UI offered no connector event timeline. The source fixture and provider receipt are in [the Core acceptance receipt](../../../opensaddle/docs/testing/receipts/personal-cli-interface-live-20260919.json).

After the change, the ordinary Work page displayed one Completed Project task row for that exact Run and opened `/project/ui-cli-acceptance/tasks/run_0714f2cbd1924c9e9a93bf5ec808bc86`. Its Connector activity panel loaded Core events 34 (`requested`) and 35 (`completed`) for `public_github/get_repository`, with the `allow` outcome and SHA-256 digests. The panel did not display action arguments, connector response bodies, credentials, or account identities. [Work screenshot](../../out/screenshots/personal-cli-20260919/work-after.png) and [task audit screenshot](../../out/screenshots/personal-cli-20260919/audit-after.png) record the browser result.

Work reads the existing membership-scoped Project directory and task feeds, at most the first 12 Projects in batches of four, with no overlapping poll within a mounted account/service scope. It keeps Core's feed statuses and exposes an unavailable/partial notice on directory or feed failure. Old reads are generation-fenced after account/service replacement or unmount. Connector inspection checks exact Project/Run and caller before and after the authenticated event read, filters to digest-only rows, and bounds the entire request to five seconds; the event stream additionally stops after 1,000 total events, 100 connector rows, or its shorter stream deadline. Pre/post status and roster reads receive the same AbortSignal. If the final authorization read cannot finish within the deadline, the audit is unavailable rather than displayed without rechecking authority. The Work screenshot and task screenshot predate this signal plumbing and minor snapshot copy change; focused tests and build cover the final code.

Validation: the focused mounted client and owning task-feed tests passed, `npm run build` passed, and `git diff --check` passed. The browser fixture used an installed noneditable Core wheel, a disposable private Project, the existing Codex login, and a private Vite proxy. The result is local browser proof; it is not a production deployment or correctness/human acceptance of the agent's response.
