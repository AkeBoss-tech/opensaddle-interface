import path from'node:path'
import{spawn,type ChildProcess}from'node:child_process'
import{createConnection}from'node:net'
import{chmodSync,mkdirSync}from'node:fs'
export interface DesktopPersonalRuntimeRequest{projectId:string;workspace:string;adapter:'codex'|'claude';executable:string;cpuMillicores:number;memoryMiB:number;maxConcurrency:number}
export interface PersonalRuntimeLaunchConfig{projectDatabase:string;stateDir:string;ipcDir:string;port:number;allowedOrigin?:string;handoffFd:number}
const text=(value:unknown,name:string)=>{if(typeof value!=='string'||!value||value.includes('\0'))throw Error(`${name} is invalid`);return value}
const integer=(value:unknown,name:string,max:number)=>{if(!Number.isSafeInteger(value)||Number(value)<1||Number(value)>max)throw Error(`${name} is invalid`);return String(value)}
const absolute=(value:unknown,name:string)=>{const result=text(value,name);if(!path.isAbsolute(result))throw Error(`${name} must be absolute`);return result}
const object=(value:unknown,name:string)=>{if(!value||typeof value!=='object'||Array.isArray(value))throw Error(`${name} is invalid`);return value as Record<string,unknown>}
export async function validatePersonalRuntimeInventory(input:{baseUrl:string;request:DesktopPersonalRuntimeRequest;fetchImpl?:typeof fetch}){const fetchImpl=input.fetchImpl??fetch,[projectResponse,harnessResponse]=await Promise.all([fetchImpl(`${input.baseUrl}/api/projects`),fetchImpl(`${input.baseUrl}/api/harnesses`)]);if(!projectResponse.ok||!harnessResponse.ok)throw Error('Local project or coding-agent inventory is unavailable');const projectPayload=object(await projectResponse.json(),'project inventory'),harnessPayload=object(await harnessResponse.json(),'coding-agent inventory');if(!Array.isArray(projectPayload.projects)||!Array.isArray(harnessPayload.harnesses))throw Error('Local project or coding-agent inventory is invalid');const project=projectPayload.projects.map(raw=>object(raw,'project')).find(raw=>raw.project_id===input.request.projectId),wantedHarness=input.request.adapter==='claude'?'claude-code':input.request.adapter,harness=harnessPayload.harnesses.map(raw=>object(raw,'coding agent')).find(raw=>raw.id===wantedHarness);if(!project||typeof project.root!=='string'||path.resolve(project.root)!==path.resolve(input.request.workspace))throw Error('Registered local project changed; refresh before commissioning');if(!harness||harness.installed!==true||!['authenticated','configured'].includes(String(harness.auth_state))||typeof harness.executable_path!=='string'||path.resolve(harness.executable_path)!==path.resolve(input.request.executable))throw Error('Installed coding agent changed; refresh before commissioning');return{projectId:input.request.projectId,harnessId:wantedHarness}}
export function preparePersonalRuntimeStateDir(stateDir:string){const resolved=absolute(stateDir,'state directory');mkdirSync(resolved,{recursive:true,mode:0o700});chmodSync(resolved,0o700);return resolved}
export function personalRuntimeLaunchArgs(request:DesktopPersonalRuntimeRequest,config:PersonalRuntimeLaunchConfig){if(!['codex','claude'].includes(request.adapter))throw Error('adapter is invalid');if(!Number.isSafeInteger(config.handoffFd)||config.handoffFd<3||config.handoffFd>1024)throw Error('handoff FD is invalid');const args=['serve-personal-runtime','--project-id',text(request.projectId,'project ID'),'--workspace',absolute(request.workspace,'workspace'),'--project-database',absolute(config.projectDatabase,'project database'),'--state-dir',absolute(config.stateDir,'state directory'),'--ipc-dir',absolute(config.ipcDir,'IPC directory'),'--host','127.0.0.1','--port',integer(config.port,'port',65535),'--adapter',request.adapter,'--executable',absolute(request.executable,'executable'),'--cpu-millicores',integer(request.cpuMillicores,'CPU',1_000_000),'--memory-mib',integer(request.memoryMiB,'memory',16_777_216),'--max-concurrency',integer(request.maxConcurrency,'concurrency',1024)];if(config.allowedOrigin)args.push('--allowed-origin',text(config.allowedOrigin,'allowed origin'));args.push('--handoff-fd',String(config.handoffFd));return args}
export interface PersonalRuntimeHandoff{baseUrl:string;installationId:string;ownerSubject:string;projectId:string;bearerToken:string;adoptionSocket:string;ipcDir:string}
export function parsePersonalRuntimeHandoff(raw:unknown,expected:{baseUrl:string;projectId:string;installationId?:string;adoptionSocket?:string;ipcDir:string}):PersonalRuntimeHandoff{if(!raw||typeof raw!=='object'||Array.isArray(raw))throw Error('Personal runtime handoff is invalid');const value=raw as Record<string,unknown>;if(Object.keys(value).sort().join(',')!=='adoption_socket,base_url,bearer_token,installation_id,ipc_dir,owner_subject,project_id,protocol_version,schema_version'||value.schema_version!=='opensaddle.personal-runtime-handoff.v1'||value.protocol_version!=='opensaddle.personal-runtime.v1')throw Error('Personal runtime handoff contract is invalid');const baseUrl=text(value.base_url,'base URL');let url:URL;try{url=new URL(baseUrl)}catch{throw Error('Personal runtime base URL is invalid')}if(url.protocol!=='http:'||url.hostname!=='127.0.0.1'||!url.port||url.pathname!=='/'||url.username||url.password||url.search||url.hash||baseUrl!==expected.baseUrl)throw Error('Personal runtime base URL must be loopback');const installationId=text(value.installation_id,'installation'),projectId=text(value.project_id,'project'),adoptionSocket=path.resolve(absolute(value.adoption_socket,'adoption socket')),ipcDir=path.resolve(absolute(value.ipc_dir,'IPC directory')),expectedIpc=path.resolve(expected.ipcDir);if(ipcDir!==expectedIpc||path.dirname(adoptionSocket)!==expectedIpc||projectId!==expected.projectId||(expected.installationId&&installationId!==expected.installationId)||(expected.adoptionSocket&&adoptionSocket!==expected.adoptionSocket))throw Error('Personal runtime handoff identity does not match commissioned runtime');return{baseUrl,installationId,ownerSubject:text(value.owner_subject,'owner'),projectId,bearerToken:text(value.bearer_token,'bearer token'),adoptionSocket,ipcDir}}
const commissions=new Map<string,{fingerprint:string;promise:Promise<{handoff:PersonalRuntimeHandoff;process:ChildProcess}>}>()
function terminateChild(child:ChildProcess){return new Promise<void>((resolve,reject)=>{if(child.exitCode!==null||child.signalCode!==null){resolve();return}let done=false,kill:ReturnType<typeof setTimeout>|undefined,confirmation:ReturnType<typeof setTimeout>|undefined;const cleanup=()=>{if(kill)clearTimeout(kill);if(confirmation)clearTimeout(confirmation);child.off('exit',finish);child.off('close',finish)},finish=()=>{if(done)return;done=true;cleanup();resolve()};child.once('exit',finish);child.once('close',finish);try{if(process.platform!=='win32'&&child.pid)process.kill(-child.pid,'SIGTERM');else child.kill('SIGTERM')}catch(error){cleanup();reject(error);return}kill=setTimeout(()=>{try{if(process.platform!=='win32'&&child.pid)process.kill(-child.pid,'SIGKILL');else child.kill('SIGKILL')}catch(error){cleanup();reject(error);return}confirmation=setTimeout(()=>{cleanup();reject(Error('Personal runtime process cleanup could not be confirmed'))},2000)},500)})}
export function commissionPersonalRuntimeProcess(input:{command:string;commandPrefix?:string[];request:DesktopPersonalRuntimeRequest;config:PersonalRuntimeLaunchConfig;expected:{baseUrl:string;projectId:string;installationId?:string;adoptionSocket?:string;ipcDir:string};timeoutMs?:number}){const key=path.resolve(input.config.stateDir),fingerprint=JSON.stringify({command:input.command,commandPrefix:input.commandPrefix??[],request:input.request,config:input.config,expected:input.expected}),active=commissions.get(key);if(active)return active.fingerprint===fingerprint?active.promise:Promise.reject(Error('A different personal runtime is already commissioned for this state'));const promise=new Promise<{handoff:PersonalRuntimeHandoff;process:ChildProcess}>((resolve,reject)=>{const args=personalRuntimeLaunchArgs(input.request,{...input.config,handoffFd:3}),child=spawn(input.command,[...(input.commandPrefix??[]),...args],{cwd:input.config.stateDir,stdio:['ignore','ignore','pipe','pipe'],detached:process.platform!=='win32'}),pipe=child.stdio[3];child.stderr?.resume();if(!pipe){void terminateChild(child).then(()=>reject(Error('Personal runtime handoff pipe is unavailable')),reject);return}let bytes='',settled=false;const cleanup=()=>{clearTimeout(timeout);pipe.off('data',data);pipe.off('error',fail);child.off('error',fail);child.off('exit',earlyExit)},finishFailure=(reason:unknown)=>{if(settled)return;settled=true;cleanup();void terminateChild(child).then(()=>reject(reason instanceof Error?reason:Error(String(reason))),cleanupError=>reject(cleanupError))},fail=(reason:unknown)=>finishFailure(reason),earlyExit=(code:number|null)=>{if(code!==0)finishFailure(Error(`Personal runtime exited before handoff (${code})`))},data=(chunk:Buffer)=>{if(settled)return;bytes+=String(chunk);if(Buffer.byteLength(bytes)>65_536){finishFailure(Error('Personal runtime handoff exceeded its bound'));return}const newline=bytes.indexOf('\n');if(newline<0)return;if(bytes.slice(newline+1).trim()){finishFailure(Error('Personal runtime handoff contains trailing data'));return}try{const handoff=parsePersonalRuntimeHandoff(JSON.parse(bytes.slice(0,newline)),input.expected);settled=true;cleanup();pipe.destroy();resolve({handoff,process:child})}catch(error){finishFailure(error)}},timeout=setTimeout(()=>finishFailure(Error('Personal runtime handoff timed out')),input.timeoutMs??15_000);child.once('error',fail);child.once('exit',earlyExit);pipe.on('data',data);pipe.once('error',fail)});commissions.set(key,{fingerprint,promise});void promise.then(({process:child})=>child.once('exit',()=>{if(commissions.get(key)?.promise===promise)commissions.delete(key)}),()=>{if(commissions.get(key)?.promise===promise)commissions.delete(key)});return promise}
export function adoptPersonalRuntime(input:{stateDir:string;ipcDir:string;socketPath:string;baseUrl:string;installationId:string;projectId:string;timeoutMs?:number}){const stateDir=path.resolve(input.stateDir),ipcDir=path.resolve(input.ipcDir),socketPath=path.resolve(input.socketPath);if(path.dirname(socketPath)!==ipcDir)return Promise.reject(Error('Personal runtime adoption socket is outside private state'));const request=JSON.stringify({schema_version:'opensaddle.personal-runtime-adoption-request.v1',expected_protocol_version:'opensaddle.personal-runtime.v1',installation_id:text(input.installationId,'installation'),project_id:text(input.projectId,'project')})+'\n';if(Buffer.byteLength(request)>1024)return Promise.reject(Error('Personal runtime adoption request exceeded its bound'));return new Promise<PersonalRuntimeHandoff>((resolve,reject)=>{const socket=createConnection(socketPath),timeout=setTimeout(()=>{socket.destroy();reject(Error('Personal runtime adoption timed out'))},input.timeoutMs??2000);let bytes='';const fail=(reason:unknown)=>{clearTimeout(timeout);socket.destroy();reject(reason instanceof Error?reason:Error(String(reason)))};socket.once('connect',()=>socket.write(request));socket.once('error',fail);socket.on('data',chunk=>{bytes+=String(chunk);if(Buffer.byteLength(bytes)>65_536){fail(Error('Personal runtime adoption response exceeded its bound'));return}const newline=bytes.indexOf('\n');if(newline<0)return;clearTimeout(timeout);socket.end();try{resolve(parsePersonalRuntimeHandoff(JSON.parse(bytes.slice(0,newline)),{baseUrl:input.baseUrl,projectId:input.projectId,installationId:input.installationId,adoptionSocket:socketPath,ipcDir}))}catch(error){fail(error)}})})}
