import assert from'node:assert/strict';import test from'node:test';import{RemoteJourneyClient}from'./remoteJourney'
test('connected journey mutations preserve authenticated project and machine scope',async()=>{const original=globalThis.fetch,calls:Array<[string,RequestInit]> = [];globalThis.fetch=async(url,init)=>{calls.push([String(url),init??{}]);return new Response(JSON.stringify({ok:true}),{status:201,headers:{'Content-Type':'application/json'}})};try{const client=new RemoteJourneyClient('https://core.example/',()=> 'owner','private');await client.createProject('project-1');await client.addMember('project-1','member-2','member');await client.registerWorker({workerId:'machine-a',organizationId:'org-1',projectIds:['project-1'],runtimeKind:'remote_worker'});await client.createRun({projectId:'project-1',sourceId:'src_12345678',task:'Review the existing change'});assert.deepEqual(calls.map(([url])=>url),['https://core.example/api/v2/projects','https://core.example/api/v2/projects/project-1/members','https://core.example/api/v2/workers','https://core.example/api/v2/runs']);assert.ok(calls.every(([,init])=>(init.headers as Record<string,string>).Authorization==='Bearer private'));assert.match(String(calls[2][1].body),/"worker_id":"machine-a"/)}finally{globalThis.fetch=original}})
test('connected journey denial never becomes a success receipt',async()=>{const original=globalThis.fetch;globalThis.fetch=async()=>new Response(JSON.stringify({detail:'project owner or admin role required'}),{status:403,headers:{'Content-Type':'application/json'}});try{await assert.rejects(new RemoteJourneyClient('https://core.example',()=> 'member','token').addMember('project-1','other','member'),/project owner or admin role required/)}finally{globalThis.fetch=original}})
test('current roster is composed only from matching authenticated projections',async()=>{const original=globalThis.fetch;globalThis.fetch=async(url)=>new Response(JSON.stringify(String(url).endsWith('/members')?{project_id:'P',members:[{subject:'owner',role:'owner',status:'active'}]}:String(url).endsWith('/workers')?{project_id:'P',workers:[{worker_id:'machine-a',runtime_kind:'remote_worker',status:'unknown'}]}:{project_id:'P',invitations:[]}),{headers:{'Content-Type':'application/json'}});try{const value=await new RemoteJourneyClient('https://core.example',()=> 'owner','token').snapshot('P');assert.equal(value.members[0].subject,'owner');assert.equal(value.workers[0].workerId,'machine-a')}finally{globalThis.fetch=original}})
test('invitation acceptance is recipient and revision bound',async()=>{const original=globalThis.fetch;let call:[string,RequestInit]|undefined;globalThis.fetch=async(url,init)=>{call=[String(url),init??{}];return new Response(JSON.stringify({status:'accepted'}),{headers:{'Content-Type':'application/json'}})};try{await new RemoteJourneyClient('https://core.example',()=> 'member','token').acceptInvitation('P','invite-1',3);assert.equal(call?.[0],'https://core.example/api/v2/projects/P/invitations/invite-1/accept');assert.equal(call?.[1].body,JSON.stringify({expected_revision:3}))}finally{globalThis.fetch=original}})
test('recipient can load its pending invitation before Project membership',async()=>{const original=globalThis.fetch;globalThis.fetch=async(url)=>String(url).endsWith('/invitations')?new Response(JSON.stringify({project_id:'P',invitations:[{invitation_id:'I',recipient_subject:'member',status:'pending',revision:1,expires_at:'later'}]}),{headers:{'Content-Type':'application/json'}}):new Response(JSON.stringify({detail:'project membership required'}),{status:403,headers:{'Content-Type':'application/json'}});try{const value=await new RemoteJourneyClient('https://core.example',()=> 'member','token').snapshot('P');assert.equal(value.rosterAvailable,false);assert.equal(value.invitations[0].invitationId,'I');assert.deepEqual(value.members,[])}finally{globalThis.fetch=original}})
test('snapshot discovers bounded Project sources and active Runs without inventing an agent choice',async()=>{const original=globalThis.fetch;globalThis.fetch=async(url)=>{const path=new URL(String(url)).pathname;if(path.endsWith('/invitations'))return Response.json({project_id:'P',invitations:[]});if(path.endsWith('/members'))return Response.json({project_id:'P',members:[{subject:'owner',role:'owner',status:'active'}]});if(path.endsWith('/workers'))return Response.json({project_id:'P',workers:[]});if(path.endsWith('/sources'))return Response.json({project_id:'P',items:[{source_id:'src-1',display_label:'git: main'}]});if(path.endsWith('/participants'))return Response.json({project_id:'P',items:[]});return Response.json({active_runs:[{project_id:'P',run_id:'run-1',task:'Inspect change',status:'running'}],outcomes:[]})};try{const value=await new RemoteJourneyClient('https://core.example',()=> 'owner','token').snapshot('P');assert.deepEqual(value.sources,[{sourceId:'src-1',label:'git: main'}]);assert.deepEqual(value.activeRuns,[{runId:'run-1',task:'Inspect change',status:'running'}])}finally{globalThis.fetch=original}})
