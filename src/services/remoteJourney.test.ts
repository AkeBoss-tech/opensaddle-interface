import assert from'node:assert/strict';import test from'node:test';import{RemoteJourneyClient}from'./remoteJourney'
test('connected journey mutations preserve authenticated project and machine scope',async()=>{const original=globalThis.fetch,calls:Array<[string,RequestInit]> = [];globalThis.fetch=async(url,init)=>{calls.push([String(url),init??{}]);return new Response(JSON.stringify({ok:true}),{status:201,headers:{'Content-Type':'application/json'}})};try{const client=new RemoteJourneyClient('https://core.example/',()=> 'owner','private');await client.createProject('project-1');await client.addMember('project-1','member-2','member');await client.registerWorker({workerId:'machine-a',organizationId:'org-1',projectIds:['project-1'],runtimeKind:'remote_worker'});await client.createRun({projectId:'project-1',sourceId:'src_12345678',task:'Review the existing change'});assert.deepEqual(calls.map(([url])=>url),['https://core.example/api/v2/projects','https://core.example/api/v2/projects/project-1/members','https://core.example/api/v2/workers','https://core.example/api/v2/runs']);assert.ok(calls.every(([,init])=>(init.headers as Record<string,string>).Authorization==='Bearer private'));assert.match(String(calls[2][1].body),/"worker_id":"machine-a"/)}finally{globalThis.fetch=original}})
test('connected journey denial never becomes a success receipt',async()=>{const original=globalThis.fetch;globalThis.fetch=async()=>new Response(JSON.stringify({detail:'project owner or admin role required'}),{status:403,headers:{'Content-Type':'application/json'}});try{await assert.rejects(new RemoteJourneyClient('https://core.example',()=> 'member','token').addMember('project-1','other','member'),/project owner or admin role required/)}finally{globalThis.fetch=original}})
test('current roster is composed only from matching authenticated projections',async()=>{const original=globalThis.fetch;globalThis.fetch=async(url)=>new Response(JSON.stringify(String(url).endsWith('/members')?{project_id:'P',members:[{subject:'owner',role:'owner',status:'active'}]}:String(url).endsWith('/workers')?{project_id:'P',workers:[{worker_id:'machine-a',runtime_kind:'remote_worker',status:'unknown'}]}:{project_id:'P',invitations:[]}),{headers:{'Content-Type':'application/json'}});try{const value=await new RemoteJourneyClient('https://core.example',()=> 'owner','token').snapshot('P');assert.equal(value.members[0].subject,'owner');assert.equal(value.workers[0].workerId,'machine-a')}finally{globalThis.fetch=original}})
test('invitation acceptance is recipient and revision bound',async()=>{const original=globalThis.fetch;let call:[string,RequestInit]|undefined;globalThis.fetch=async(url,init)=>{call=[String(url),init??{}];return new Response(JSON.stringify({status:'accepted'}),{headers:{'Content-Type':'application/json'}})};try{await new RemoteJourneyClient('https://core.example',()=> 'member','token').acceptInvitation('P','invite-1',3);assert.equal(call?.[0],'https://core.example/api/v2/projects/P/invitations/invite-1/accept');assert.equal(call?.[1].body,JSON.stringify({expected_revision:3}))}finally{globalThis.fetch=original}})
test('recipient can load its pending invitation before Project membership',async()=>{const original=globalThis.fetch;globalThis.fetch=async(url)=>String(url).endsWith('/invitations')?new Response(JSON.stringify({project_id:'P',invitations:[{invitation_id:'I',recipient_subject:'member',status:'pending',revision:1,expires_at:'later'}]}),{headers:{'Content-Type':'application/json'}}):new Response(JSON.stringify({detail:'project membership required'}),{status:403,headers:{'Content-Type':'application/json'}});try{const value=await new RemoteJourneyClient('https://core.example',()=> 'member','token').snapshot('P');assert.equal(value.rosterAvailable,false);assert.equal(value.invitations[0].invitationId,'I');assert.deepEqual(value.members,[])}finally{globalThis.fetch=original}})
test('snapshot discovers bounded Project sources and active Runs without inventing an agent choice',async()=>{const original=globalThis.fetch;globalThis.fetch=async(url)=>{const path=new URL(String(url)).pathname;if(path.endsWith('/invitations'))return Response.json({project_id:'P',invitations:[]});if(path.endsWith('/members'))return Response.json({project_id:'P',members:[{subject:'owner',role:'owner',status:'active'}]});if(path.endsWith('/workers'))return Response.json({project_id:'P',workers:[]});if(path.endsWith('/sources'))return Response.json({project_id:'P',items:[{source_id:'src-1',display_label:'git: main'}]});if(path.endsWith('/participants'))return Response.json({project_id:'P',items:[]});return Response.json({active_runs:[{project_id:'P',run_id:'run-1',task:'Inspect change',status:'running'}],outcomes:[]})};try{const value=await new RemoteJourneyClient('https://core.example',()=> 'owner','token').snapshot('P');assert.deepEqual(value.sources,[{sourceId:'src-1',label:'git: main'}]);assert.deepEqual(value.activeRuns,[{runId:'run-1',task:'Inspect change',status:'running'}])}finally{globalThis.fetch=original}})
test('result cards use the authoritative Run task and worker instead of duplicate projection titles',async()=>{const original=globalThis.fetch;globalThis.fetch=async(url)=>{const path=new URL(String(url)).pathname;if(path.endsWith('/invitations'))return Response.json({project_id:'P',invitations:[]});if(path.endsWith('/members'))return Response.json({project_id:'P',members:[]});if(path.endsWith('/workers'))return Response.json({project_id:'P',workers:[]});if(path.endsWith('/sources')||path.endsWith('/participants'))return Response.json({project_id:'P',items:[]});if(path.endsWith('/runs/run-1'))return Response.json({project_id:'P',run_id:'run-1',task:'Review auth boundary',status:'completed',assigned_worker_id:'machine-a',updated_at:'2026-09-07T19:00:00Z'});return Response.json({active_runs:[],outcomes:[{project_id:'P',run_id:'run-1',title:'Run completed with artifact evidence',verified:false,artifact_available:true}]})};try{const value=await new RemoteJourneyClient('https://core.example',()=> 'member','token').snapshot('P');assert.deepEqual(value.results,[{runId:'run-1',title:'Review auth boundary',verified:false,artifactAvailable:true,workerId:'machine-a',status:'completed',updatedAt:'2026-09-07T19:00:00Z'}])}finally{globalThis.fetch=original}})
test('capacity transport reads exact Project status and configures limits with current caller authority',async()=>{const original=globalThis.fetch,requests:Array<{path:string;method:string;body?:unknown;user:string|null}>=[];globalThis.fetch=async(input,init)=>{const request=new Request(input,init),path=new URL(request.url).pathname;requests.push({path,method:request.method,body:request.method==='PUT'?JSON.parse(await request.clone().text()):undefined,user:request.headers.get('X-OpenSaddle-User')});if(path.endsWith('/invitations'))return Response.json({project_id:'P',invitations:[]});if(path.endsWith('/members'))return Response.json({project_id:'P',members:[{subject:'owner',role:'owner',status:'active'}]});if(path.endsWith('/workers'))return Response.json({project_id:'P',workers:[]});if(path.endsWith('/sources')||path.endsWith('/participants'))return Response.json({project_id:'P',items:[]});if(path.endsWith('/capacity'))return Response.json({schema_version:'opensaddle.resource-capacity.v1',project_id:'P',accounting_enforced:true,state:'configured',admission_state:'idle',overcommit:null,limits:{cpu_millicores:2000,memory_mib:4096,max_concurrency:2},revision:1,usage:{cpu_millicores:500,memory_mib:1024,concurrency:1},available:{cpu_millicores:1500,memory_mib:3072,concurrency:1},workers:[],reservations:[],recent_releases:[],queued_admission:[],generated_at:'2026-09-07T22:00:00Z',pending_phases:['monetary_budgets','interactive_reserve','fairness']});if(path.endsWith('/capacity-limits'))return Response.json({schema_version:'opensaddle.resource-capacity.v1',project_id:'P',cpu_millicores:3000,memory_mib:6000,max_concurrency:3,revision:2});return Response.json({active_runs:[],outcomes:[]})};try{const client=new RemoteJourneyClient('https://core.example',()=> 'owner','private',true),snapshot=await client.snapshot('P');assert.deepEqual(snapshot.capacity?.available,{cpuMillicores:1500,memoryMiB:3072,concurrency:1});await client.configureCapacity('P',{cpuMillicores:3000,memoryMiB:6000,maxConcurrency:3});assert.deepEqual(requests.at(-1),{path:'/api/v2/projects/P/capacity-limits',method:'PUT',body:{cpu_millicores:3000,memory_mib:6000,max_concurrency:3},user:'owner'});assert.ok(requests.every(item=>item.user==='owner'))}finally{globalThis.fetch=original}})
test('malformed capacity never becomes invented zero usage',async()=>{const original=globalThis.fetch;globalThis.fetch=async(input)=>{const path=new URL(String(input)).pathname;if(path.endsWith('/invitations'))return Response.json({project_id:'P',invitations:[]});if(path.endsWith('/members'))return Response.json({project_id:'P',members:[{subject:'member',role:'member',status:'active'}]});if(path.endsWith('/workers'))return Response.json({project_id:'P',workers:[]});if(path.endsWith('/sources')||path.endsWith('/participants'))return Response.json({project_id:'P',items:[]});if(path.endsWith('/capacity'))return Response.json({schema_version:'opensaddle.resource-capacity.v1',project_id:'P',state:'configured',admission_state:'idle',limits:{},usage:null,available:null,pending_phases:[]});return Response.json({active_runs:[],outcomes:[]})};try{const snapshot=await new RemoteJourneyClient('https://core.example',()=> 'member',undefined,true).snapshot('P');assert.equal(snapshot.capacity,undefined);assert.match(snapshot.capacityError!,/enforcement is invalid/)}finally{globalThis.fetch=original}})
test('established member reads the Project when invitation administration is denied',async()=>{const original=globalThis.fetch;globalThis.fetch=async(input)=>{const path=new URL(String(input)).pathname;if(path.endsWith('/invitations'))return Response.json({detail:'invitation recipient or project owner required'},{status:403});if(path.endsWith('/members'))return Response.json({project_id:'P',members:[{subject:'member',role:'member',status:'active'}]});if(path.endsWith('/workers'))return Response.json({project_id:'P',workers:[]});if(path.endsWith('/sources')||path.endsWith('/participants'))return Response.json({project_id:'P',items:[]});return Response.json({active_runs:[],outcomes:[]})};try{const snapshot=await new RemoteJourneyClient('https://core.example',()=> 'member').snapshot('P');assert.equal(snapshot.rosterAvailable,true);assert.deepEqual(snapshot.invitations,[]);assert.equal(snapshot.canManage,false)}finally{globalThis.fetch=original}})

test('native readiness is strictly source-bound and selected adapter is sent exactly',async()=>{const original=globalThis.fetch,calls:Array<{path:string;body?:unknown}>=[];globalThis.fetch=async(input,init)=>{const request=new Request(input,init),path=new URL(request.url).pathname;calls.push({path,body:request.method==='POST'?JSON.parse(await request.clone().text()):undefined});if(path.endsWith('/invitations'))return Response.json({project_id:'P',invitations:[]});if(path.endsWith('/members'))return Response.json({project_id:'P',members:[{subject:'member',role:'member',status:'active'}]});if(path.endsWith('/workers'))return Response.json({project_id:'P',workers:[]});if(path.endsWith('/sources'))return Response.json({project_id:'P',items:[{source_id:'src_12345678',display_label:'snapshot'}]});if(path.endsWith('/participants'))return Response.json({project_id:'P',items:[]});if(path.endsWith('/native-adapters'))return Response.json({schema_version:'opensaddle.native-adapter-readiness.v1',project_id:'P',generated_at:'2026-09-07T23:00:00Z',items:[{schema_version:'opensaddle.native-adapter-readiness.v1',worker_id:'machine-a',project_id:'P',adapter_id:'codex-app-server',source_id:'src_12345678',revision:'rev-1',digest:'d'.repeat(64),executable_state:'installed',executable_version:'0.147.0',authentication_state:'authenticated',account_mode:'chatgpt',protocol_state:'compatible',protocol_version:'app-server-v1',workspace_state:'configured',ready:true,reason:null,observed_at:'2026-09-07T23:00:00Z',expires_at:'2026-09-07T23:02:00Z',reported_at:'2026-09-07T23:00:01Z'}]});if(path==='/api/v2/runs')return Response.json({run_id:'run-1'},{status:201});return Response.json({active_runs:[],outcomes:[]})};try{const client=new RemoteJourneyClient('https://core.example',()=> 'member','token',false,true),snapshot=await client.snapshot('P');assert.equal(snapshot.nativeAdapters?.[0].ready,true);assert.equal(snapshot.nativeAdapters?.[0].sourceId,'src_12345678');await client.delegate('P','src_12345678','Read fact','codex-app-server');assert.deepEqual(calls.at(-1),{path:'/api/v2/runs',body:{project_id:'P',source_id:'src_12345678',task:'Read fact',native_adapter_id:'codex-app-server'}})}finally{globalThis.fetch=original}})

test('malformed native readiness cannot synthesize an available agent',async()=>{const original=globalThis.fetch;globalThis.fetch=async(input)=>{const path=new URL(String(input)).pathname;if(path.endsWith('/invitations'))return Response.json({project_id:'P',invitations:[]});if(path.endsWith('/members'))return Response.json({project_id:'P',members:[{subject:'member',role:'member',status:'active'}]});if(path.endsWith('/workers'))return Response.json({project_id:'P',workers:[]});if(path.endsWith('/sources')||path.endsWith('/participants'))return Response.json({project_id:'P',items:[]});if(path.endsWith('/native-adapters'))return Response.json({schema_version:'opensaddle.native-adapter-readiness.v1',project_id:'P',generated_at:'later',items:[{ready:true}]});return Response.json({active_runs:[],outcomes:[]})};try{const snapshot=await new RemoteJourneyClient('https://core.example',()=> 'member',undefined,false,true).snapshot('P');assert.equal(snapshot.nativeAdapters,undefined);assert.match(snapshot.nativeAdaptersError!,/generation time is invalid/)}finally{globalThis.fetch=original}})
