type RuntimeIdentity={baseUrl:string;installationId:string;projectId:string}
let installed:RuntimeIdentity|undefined,originalFetch:typeof fetch|undefined
export function installPersonalRuntimeTransport(identity:RuntimeIdentity){if(!window.opensaddle?.personalRuntimeRequest)return;const base=new URL(identity.baseUrl);if(base.protocol!=='http:'||base.hostname!=='127.0.0.1'||!base.port||base.pathname!=='/'||base.search||base.hash)throw Error('Personal runtime transport endpoint is invalid');installed={...identity};if(originalFetch)return;originalFetch=globalThis.fetch.bind(globalThis);globalThis.fetch=async(input,init)=>{const request=new Request(input,init),url=new URL(request.url),authority=installed;if(!authority||url.origin!==new URL(authority.baseUrl).origin)return originalFetch!(input,init);if(request.headers.has('Authorization'))throw Error('Personal runtime credentials are owned by the desktop transport');if(request.signal.aborted)throw request.signal.reason??new DOMException('Aborted','AbortError');const method=request.method as'GET'|'POST'|'PUT',body=method==='GET'?undefined:await request.text(),pending=window.opensaddle!.personalRuntimeRequest!({path:`${url.pathname}${url.search}`,method,expectedBaseUrl:authority.baseUrl,expectedInstallationId:authority.installationId,expectedProjectId:authority.projectId,...(body?{body}:{})}),result=await(request.signal?Promise.race([pending,new Promise<never>((_,reject)=>request.signal.addEventListener('abort',()=>reject(request.signal.reason??new DOMException('Aborted','AbortError')),{once:true}))]):pending);if(installed?.installationId!==authority.installationId||installed.projectId!==authority.projectId||installed.baseUrl!==authority.baseUrl)throw Error('Personal runtime authority changed while the request was pending');const bytes=Uint8Array.from(atob(result.bodyBase64),character=>character.charCodeAt(0));return new Response([204,205,304].includes(result.status)?null:bytes,{status:result.status,headers:{'Content-Type':result.contentType}})}}
