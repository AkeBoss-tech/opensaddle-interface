import assert from'node:assert/strict';import test from'node:test';import{createPersonalRuntimeCommissionRequest}from'./personalRuntimeCommissioning';import type{HarnessCapability}from'./contracts'
const harness=(id:'codex'|'claude',ready=true)=>({id,label:id,description:id,kind:'native',availability:ready?'available':'missing',readiness:ready?'ready':'unavailable',resolvedPath:ready?`/opt/${id}`:undefined,auth:{state:'configured'},models:[],capabilities:{streaming:true,tools:true,mcp:true,skills:true,reasoningControls:true,contextMetadata:true,cancellation:true,policyControls:'native'}} as HarnessCapability)
test('commission request binds registered root ready executable and explicit resources',()=>assert.deepEqual(createPersonalRuntimeCommissionRequest({projectId:'P',provider:'codex',cpu:'2000',memory:'4096',concurrency:'1'},[{projectId:'P',root:'/repo',createdAt:1}],[harness('codex')]),{projectId:'P',workspace:'/repo',adapter:'codex',executable:'/opt/codex',cpuMillicores:2000,memoryMiB:4096,maxConcurrency:1}))
test('commission request rejects invented projects unavailable providers and malformed resources',()=>{assert.throws(()=>createPersonalRuntimeCommissionRequest({projectId:'missing',provider:'codex',cpu:'1',memory:'1',concurrency:'1'},[],[harness('codex')]),/registered/);assert.throws(()=>createPersonalRuntimeCommissionRequest({projectId:'P',provider:'claude',cpu:'1',memory:'1',concurrency:'1'},[{projectId:'P',root:'/repo',createdAt:1}],[harness('claude',false)]),/not installed/);assert.throws(()=>createPersonalRuntimeCommissionRequest({projectId:'P',provider:'codex',cpu:'0',memory:'1.5',concurrency:'1'},[{projectId:'P',root:'/repo',createdAt:1}],[harness('codex')]),/CPU/)})

test('commissioning rejects concurrency the personal worker cannot run',()=>assert.throws(()=>createPersonalRuntimeCommissionRequest({projectId:'P',provider:'codex',cpu:'2000',memory:'4096',concurrency:'2'},[{projectId:'P',root:'/repo',createdAt:1}],[harness('codex')]),/Concurrency/))
