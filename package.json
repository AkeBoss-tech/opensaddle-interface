{
  "name": "opensaddle-interface",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "npm exec --prefix packages/control-plane -- tsx --test test/*.test.ts",
    "build": "tsc -b && vite build",
    "lint": "oxlint src",
    "preview": "vite preview",
    "typecheck": "tsc -b --pretty false",
    "server": "npm run start --prefix packages/control-plane",
    "server:dev": "npm run dev --prefix packages/control-plane",
    "server:build": "npm run build --prefix packages/control-plane",
    "server:test": "npm test --prefix packages/control-plane",
    "server:typecheck": "npm run typecheck --prefix packages/control-plane",
    "real:check": "npm run typecheck && npm run lint && npm run test && npm run server:typecheck && npm run server:test && npm run build && npm run server:build",
    "krail": "npm run start --prefix packages/krail",
    "electron:build": "npm run build --prefix electron",
    "electron:dev": "VITE_APP_BASE=./ npm run build && npm run build --prefix electron && npm run start --prefix electron",
    "desktop": "npm run electron:dev",
    "desktop:package": "VITE_APP_BASE=./ npm run build && npm run package:mac --prefix electron"
  },
  "dependencies": {
    "@heroicons/react": "^2.2.0",
    "@phosphor-icons/react": "^2.1.10",
    "@tabler/icons-react": "^3.46.0",
    "lucide-react": "^1.27.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "react-router-dom": "^7.18.1"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "oxlint": "^1.71.0",
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  }
}
